import 'package:flutter/material.dart';

class KeyPage extends StatelessWidget {
  const KeyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Gift')));
  }
}
